#include<stdio.h>
#include<stdlib.h>
#include<string.h>

void main(){

	//struct OTT*ptr=(struct 0TT*)malloc(size of(struct OTT));
	
	int *ptr=(int*)malloc(sizeof(int));

	*ptr1=50;

	printf("%d",*ptr1);

}
