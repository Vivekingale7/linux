#include<stdio.h>
 void main(){
	 int x=10;
	 int y=10;

	 if(x>y){
		 printf("max is %d \n",x);
	 }
	 if (x<y){
		 printf("max is %d\n",y);
	 }
	 if (x>=y){
		 printf("max is %d\n",x);
	 }
	 if(x<=y){
		 printf("max is %d \n",y);
	 }
	if (x==y){
		 printf("x and y both have same value");
	 }
 }
