#include<stdio.h>
 void main(){
	 int x;
	 printf("enter the value :");
	 scanf("%d",&x);

	 if(x=0){
		 printf("%d the value is zero",x);
	 }
	 
	 if(x=1){
		 printf("%d the value is zero",x);
	 }
	 if(x=2){
		 printf("%d the value is zero",x);
	 }
	 if(x=3){
		 printf("%d the value is zero",x);
	 }
	 if(x=4){
		 printf("%d the value is zero",x);
	 }
	 if(x=5){
		 printf("%d the value is zero",x);
	 }
	 if(x=6){
		 printf("%d the value is zero",x);
	 }
	 else{
		 printf("%d the value is not available",x);

	 }
}


































































